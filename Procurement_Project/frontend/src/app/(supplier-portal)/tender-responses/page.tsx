"use client";
import { EmptyState, LoadingState, StatusBadge } from "@/components/ui/portal-ui";
import { api, collectionFrom } from "@/lib/api";
import { formatDate, formatMoney } from "@/lib/formatters";
import { useEffect, useState } from "react";
type Row = { id: number; receipt_number: string | null; quotation_number: string; total_amount: string; status: string; submitted_at: string | null; tender: { tender_number: string; title: string } };
export default function ResponsesPage() { const [rows, setRows] = useState<Row[] | null>(null); useEffect(() => { void api("supplier-portal/tender-responses").then((value) => setRows(collectionFrom(value).rows as Row[])); }, []); return <div className="supplier-page"><header className="supplier-page-heading"><div><span className="eyebrow">Submission history</span><h1>My tender responses</h1><p>Only responses owned by your supplier account are shown.</p></div></header><section className="supplier-panel">{rows === null ? <LoadingState /> : rows.length === 0 ? <EmptyState title="No tender responses" copy="Draft and submitted quotations will appear here." /> : <div className="portal-table-wrap"><table className="portal-table"><thead><tr><th>Tender</th><th>Quotation</th><th>Receipt</th><th>Total</th><th>Submitted</th><th>Status</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td><strong>{row.tender.tender_number}</strong><small>{row.tender.title}</small></td><td>{row.quotation_number}</td><td>{row.receipt_number ?? "Draft"}</td><td>{formatMoney(row.total_amount)}</td><td>{formatDate(row.submitted_at)}</td><td><StatusBadge status={row.status} /></td></tr>)}</tbody></table></div>}</section></div>; }
