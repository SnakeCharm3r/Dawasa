"use client";
import { useAuth } from "@/components/auth-provider";
import { ApiError } from "@/lib/api";
import { Building2, LoaderCircle, LockKeyhole, Mail } from "lucide-react";
import Link from "next/link";
import { FormEvent, useState } from "react";
export function SupplierLoginForm() { const { login, loading, user } = useAuth(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit(e: FormEvent) { e.preventDefault(); setBusy(true); setError(""); try { await login(email, password, false); } catch (caught) { setError(caught instanceof ApiError ? caught.message : "Unable to sign in."); setBusy(false); } } if (loading || user) return <LoadingCircle />; return <div className="supplier-login-card"><span className="login-emblem"><Building2 size={26} /></span><span className="eyebrow">Secure supplier access</span><h1>Supplier login</h1><p>Manage your company profile, documents, invitations and quotations.</p><form onSubmit={submit}>{error && <div className="portal-alert error">{error}</div>}<label className="portal-field"><span>Company email</span><div className="portal-input-icon"><Mail size={17} /><input type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /></div></label><label className="portal-field"><span>Password</span><div className="portal-input-icon"><LockKeyhole size={17} /><input type="password" value={password} onChange={(e) => setPassword(e.target.value)} required /></div></label><button className="portal-button primary wide" disabled={busy}>{busy && <LoaderCircle className="spin" size={17} />}Sign in</button></form><small>Not registered? <Link href="/supplier-registration">Become a supplier</Link></small></div>; }
function LoadingCircle() { return <div className="portal-state"><LoaderCircle className="spin" /></div>; }
